updateTask:
  handler: src/handlers/updateTask.handler
  events:
    - http:
        path: tasks/{id}
        method: put
        private: true
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:UpdateItem
      Resource: !GetAtt TasksTable.Arn
