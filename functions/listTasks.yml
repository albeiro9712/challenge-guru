listTasks:
  handler: src/handlers/listTasks.handler
  events:
    - http:
        path: tasks
        method: get
        private: true
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:Scan
      Resource: !GetAtt TasksTable.Arn
