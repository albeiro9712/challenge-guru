getTask:
  handler: src/handlers/getTask.handler
  events:
    - http:
        path: tasks/{id}
        method: get
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:GetItem
      Resource: !GetAtt TasksTable.Arn
