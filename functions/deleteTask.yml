deleteTask:
  handler: src/handlers/deleteTask.handler
  events:
    - http:
        path: tasks/{id}
        method: delete
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:DeleteItem
      Resource: !GetAtt TasksTable.Arn
