createTask:
  handler: src/handlers/createTask.handler
  events:
    - http:
        path: tasks
        method: post
        private: true
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:PutItem
      Resource: !GetAtt TasksTable.Arn
