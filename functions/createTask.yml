createTask:
  handler: src/handlers/createTask.handler
  events:
    - http:
        path: tasks
        method: post
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:PutItem
      Resource: !GetAtt TasksTable.Arn
